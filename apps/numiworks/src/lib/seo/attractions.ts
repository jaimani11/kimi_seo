/**
 * Shim — attraction pages data lives in @adored/seo-data.
 */
export type { Attraction } from '@adored/seo-data';
export {
  ATTRACTIONS,
  findAttractionBySlug,
  allAttractions,
  cityFor,
} from '@adored/seo-data';
