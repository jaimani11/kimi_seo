/** Shim — destination photo resolver lives in @adored/imagery. */
export type {
  DestinationPhotoQuery,
  ResolvedDestinationPhoto,
} from '@adored/imagery';
export { resolveDestinationPhoto } from '@adored/imagery';
